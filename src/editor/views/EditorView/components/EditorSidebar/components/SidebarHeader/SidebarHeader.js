// @flow
import React from 'react';
import * as styles from './styles';

const SidebarHeader = () => (
  <header className={styles.headerClass}>
    <div className={styles.headingClass}>TODO...</div>
  </header>
);

export default SidebarHeader;
